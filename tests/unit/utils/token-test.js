import token from 'sugar-portal/utils/token';
import { module, test } from 'qunit';

module('Unit | Utility | token');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = token();
  assert.ok(result);
});
